def fibo(limit):
    a, b = 0, 1
    fiblist =[]
    while b < limit:
        a, b = b, a + b
        fiblist.append(a)
        print sum(num for num in fiblist if num % 2 == 0 )
rep=int(input())
for repea in range(rep):
    limit=int(input())
    ans=fibo(limit)
    print(ans)
